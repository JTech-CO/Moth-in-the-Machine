# Moth in the Machine 저장소 불변식

이 파일은 `docs/Moth in the Machine_하네스.md`가 참조하는 루트 전역 규칙과 불변식을 백서 기준으로 명시한다.

## 작업 순서

- M1 → M2 → M3 → M4 → M5 → M6 → M7 → M8 → M9 순서를 지킨다.
- 현재 phase의 DoD와 검증 게이트가 모두 통과하기 전 다음 phase 구현을 시작하지 않는다.
- 테스트나 게이트를 약화해 실패를 숨기지 않는다.
- 세션 종료 전에 `PROGRESS.md`를 현재 상태로 갱신한다.

## 아키텍처 경계

- `utils`는 React, UI, hooks, store에 의존하지 않는 결정론적 순수 로직이다.
- `store`는 components/pages에 의존하지 않는다.
- `hooks`는 store와 utils를 UI에 연결할 수 있지만 components/pages에 의존하지 않는다.
- `components`와 `pages`는 store를 직접 import하지 않고 hooks를 통해 읽고 갱신한다.
- 공용 경로 별칭은 `@/*` → `src/*`이다.

## 게임 도메인 불변식

- 체력은 항상 0 이상 100 이하이고, 0이면 즉시 실패한다.
- 목표까지 유클리드 거리가 0.5 unit 이하이고 속도가 정지 임계값 이하일 때만 착지 성공이다.
- 남은 체력 100은 별 3개, 50 이상은 2개, 0 초과는 1개, 0은 실패다.
- HUD, 결과 화면, 공유 결과는 동일한 store 값을 단일 소스로 사용한다.
- 프레임 단위 로직은 delta time을 사용하고 NaN 및 무한대 값을 허용하지 않는다.

## 제품 제약

- 다크 테마 고정이며 1947년 산업·과학 연구소 미학을 유지한다. Windows XP와 스팀펑크 표현은 사용하지 않는다.
- 외부 API나 백엔드 없이 오프라인 우선으로 동작한다.
- 시크릿, `.env`, 원본 대용량 모델 및 바이너리를 저장소에 넣지 않는다.
- glTF 자산을 도입할 때는 Draco 압축본을 사용하고 fallback geometry를 제공한다.
- 접근 가능한 키보드 포커스와 색상 이외의 텍스트 정보를 제공한다.
